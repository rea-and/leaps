# Leaps Quick Record

Open the app once, confirm the backend URL, then add **Leaps Quick Record** from the Android widget picker.

The widget records a Medium post read or daily pills taken with one tap. The debug APK is produced at `app/build/outputs/apk/debug/app-debug.apk`.
